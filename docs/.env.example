# OpenRouter — диалоги (ChatGPT) и картинки; обязателен для генерации текста и изображений
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_TEXT_MODEL=openai/gpt-5.4

# Картинки в пузырях чата (4:3). Рекомендация: google/gemini-2.5-flash-image
OPENROUTER_IMAGE_MODEL=google/gemini-2.5-flash-image

# Story-кадры 9:16 (детализированная иллюстрация + parallax). Рекомендация: google/gemini-2.5-flash-image (~$0.04/кадр)
# Сравнить модели: npm run compare:images
OPENROUTER_STORY_IMAGE_MODEL=google/gemini-2.5-flash-image

OPENROUTER_TTS_MODEL=google/gemini-3.1-flash-tts-preview
OPENROUTER_TTS_VOICE_FEMALE=Leda
OPENROUTER_TTS_VOICE_MALE=Puck
OPENROUTER_VIDEO_MODEL=google/veo-3.1-lite
# Разрешение story-видео: 720p или 1080p (по умолчанию 1080p)
OPENROUTER_VIDEO_RESOLUTION=1080p
# Story-видео: veo (Veo 3.1 Lite через OpenRouter, на Mac) | local-gpu (Wan на :8008, опционально)
STORY_VIDEO_PROVIDER=veo
# Render-воркер (порт 3333) — LAN Mac/PC или облачный GPU
LOCAL_GPU_RENDER_URL=http://192.168.0.136:3333
# LOCAL_GPU_RENDER_URL=http://<gpu-server>:3333
LOCAL_GPU_RENDER_DEFAULT=1
# Опционально: gpu-service :8008 (Wan/FLUX) — только если STORY_*_PROVIDER=local-gpu
# LOCAL_GPU_VIDEO_URL=http://<gpu-server>:8008
# LOCAL_GPU_RENDER_AUTO=1
# LOCAL_GPU_RENDER_PORT=3333
# Story-кадры 9:16 на Mac: openrouter (Gemini, рекомендуется)
STORY_IMAGE_PROVIDER=openrouter
# Публичный HTTPS URL UI (опционально; иначе картинки уходят в OpenRouter как data URL)
PUBLIC_BASE_URL=
OPENROUTER_IMAGE_ASPECT_RATIO=4:3
# Story 9:16 — 2K даёт больше деталей на Gemini Flash (1K для экономии)
OPENROUTER_STORY_IMAGE_SIZE=2K
OPENROUTER_IMAGE_SIZE=1K
OPENROUTER_APP_NAME=messanger

# Depth parallax: auto (V2 в .venv на Mac) | depth-v2 | xenova (Docker CPU)
# Нативный воркер: ./run.sh setup-native && ./run.sh worker-native
# STORY_DEPTH_PROVIDER=auto
# STORY_DEPTH_V2_MODEL=depth-anything/Depth-Anything-V2-Large-hf
# Параллельных parallax bake (opencv + ffmpeg). По умолчанию 2–6 по числу CPU; на GPU-воркере — 4
# STORY_DEPTH_BAKE_CONCURRENCY=4

# YouTube — автозагрузка Shorts (refresh token через OAuth Playground)
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=
YOUTUBE_REFRESH_TOKEN=
